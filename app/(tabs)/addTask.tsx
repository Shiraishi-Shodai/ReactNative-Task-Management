import React from "react";
import AddTaskFormWrapper from "@/components/AddTaskFormWrapper";

// FIXME: 不要なコンポーネントのレンダリングを修正する(日付や時計のボタンを押すと不要なレンダリングが発生している)

const AddTask = () => {
  return <AddTaskFormWrapper />;
};

export default AddTask;
